<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Purchase Request Form -->
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button name="action_submit" string="Submit" type="object" invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_approve" string="Approve" type="object" invisible="state != 'submitted'" class="btn-success"/>
                    <button name="action_reject" string="Reject" type="object" invisible="state != 'submitted'" class="btn-danger"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="employee_id"/>
                        <field name="request_date"/>
                    </group>
                    <group>
                        <field name="reason" placeholder="Explain why this purchase is needed..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    
    <!-- Purchase Request List -->
    <record id="view_purchase_request_list" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list string="Purchase Requests">
                <field name="name"/>
                <field name="employee_id"/>
                <field name="request_date"/>
                <field name="state"/>
            </list>
        </field>
    </record>
    
    <!-- Purchase Request Action -->
    <record id="action_purchase_request" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,form</field>
    </record>
    
    <!-- Purchase Request Menu -->
    <menuitem id="menu_purchase_request" 
              name="Purchase Requests" 
              parent="purchase.menu_procurement_management" 
              action="action_purchase_request"
              sequence="5"/>
</odoo>
